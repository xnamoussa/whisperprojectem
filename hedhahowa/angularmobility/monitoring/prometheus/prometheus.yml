global:
  scrape_interval: 10s
  evaluation_interval: 10s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: mobility-ml-api
    metrics_path: /metrics
    static_configs:
      - targets: ["ml-api:8000"]
